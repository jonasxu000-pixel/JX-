# V1.0 发布候选版验收清单

> 发布条件：自动门禁全部通过，外部配置已确认，双真机体验项全部通过。

## 自动门禁

- [x] 房间创建、加入、双方胜负、并发落子、并发加入验证通过
- [x] 页面 watch 同步、回合锁定、双方胜负视角验证通过
- [x] 房间监听错误后退避重连，切后台恢复时重新同步
- [x] 联机对局结束后支持同房间“再来一局”和返回首页
- [x] 满盘无胜者时以和棋结束
- [x] 普通用户界面不展示诊断入口
- [x] 云函数诊断动作默认关闭，仅设置 `ENABLE_ROOM_DIAGNOSTICS=true` 时启用
- [x] 全项目 JavaScript 语法检查通过
- [x] 发布配置 JSON 可解析，URL 校验和代码压缩已开启
- [x] 本地 `.codex-preview` 验收产物已从发布包排除

执行命令：

```powershell
node scripts/verify-release.js
```

线上云环境验证命令（先通过开发者工具 CLI 开启自动化端口 `9420`）：

```powershell
node scripts/verify-cloud-release.js
```

## 外部配置门禁

- [x] 云数据库 `rooms` 权限已设置为 `read: true`、`write: false`，真实客户端直写返回 `DATABASE_PERMISSION_DENIED`
- [x] 已部署最新 `roomAction`，真实调用返回版本 `roomAction-20260611-release-candidate-2`
- [x] 生产诊断动作已关闭，真实调用 `selfTestMove` 返回“诊断操作未启用”
- [x] 云端创建房间与离开清理链路通过
- [x] 云端拒绝未结束房间执行“再来一局”
- [x] 微信开发者工具预览构建通过，包体 `32.6 KB`

## 双真机体验门禁

- [x] 第 1 局：房主获胜，双方胜负文案正确，最终棋子先显示再弹窗
- [x] 第 2 局：加入者获胜，双方胜负文案正确
- [x] 第 3 局：双方快速连续点击，无重复落子、错回合或卡死
- [x] 第 4 局：双方分别切后台再恢复，棋盘和回合一致
- [x] 第 5 局：一方切换 Wi-Fi / 移动网络后继续完成对局
- [x] 同房间“再来一局”真机复测
- [x] 对手中途离开时，另一方收到明确状态

## 发布结论

- 当前状态：**V1.0 发布候选版稳定性验收通过**
- 阻断规则：任一门禁失败，不提交正式发布
- 外部配置门禁已全部通过
